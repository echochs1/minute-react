import React from "react";
import { Button } from "antd";

const Record = () => {
    return (
        <div>
            <h1>Record</h1>
            <Button type="primary">1-min word challenge</Button>
            <Button type="primary">Topic selector</Button>
        </div>
    );
}

export default Record;